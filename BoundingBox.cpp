#include "BoundingBox.h"

bool BoundingBox::Intersect(std::vector<float> OrigoPoints)
{ 

	///AABB --> Axis Align Collision
	for (auto points : OrigoPoints) 
	{
	
		/*if (abs(a.center.a - b.center.x) > (a.radius.x + b.radius.x)) return 0;
		if (abs(a.center.y - b.center.y) > (a.radius.y + b.radius.y)) return 0;
		if (abs(a.center.z - b.center.z) > (a.radius.z + b.radius.z)) return 0;*/
	}
	

	return false;
}
